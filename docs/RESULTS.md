# Run 1 — 20 steps, Qwen3.5-0.8B vs Pangram

Self-hosted prime-rl 0.7.0 on Modal (2x A100-40GB), `configs/rl.toml` + `configs/eval.toml`,
commit `83295db`. W&B run `selfhosted-main` / `a0f1a63c`.

## Headline

The policy learned to evade the detector, and the craft rubric did not detect a cost.

| | step 1 (base) | step 10 | step 20 | step 21* |
|---|---|---|---|---|
| escaped `<0.9` | 2/40 — 5% | 0/25 | 14/31 — 45% | 20/38 — **53%** |
| escaped `<0.5` | 0/40 | 0/25 | 5/31 — 16% | 8/38 — **21%** |
| mean `ai_score` | 0.985 | 0.988 | 0.784 | **0.749** |
| min `ai_score` | 0.836 | 0.951 | 0.067 | 0.108 |

\* step 21 is the pipelined batch generated by the final policy and never trained on — the
cleanest read of the end state. prime-rl generates step N+1 while training on N.

A 10x rise against a 5% baseline that matched calibration's 7.8%. Flat through step ~8, first
signal at 11, sharp break at 19-21. Roughly a third of steps produced zero escapes: with a
step-function reward those groups are uniform, carry zero advantage, and teach nothing while
still costing $0.05/rollout.

## Craft: no detectable cost

The eval overlay measured nothing (see below), so the rubric was run offline against the
rollouts on disk, 30 per cohort, judged by `openai/gpt-5.4-mini` ($1.31 total).

| cohort | n | craft | sd | mean ai_score |
|---|---|---|---|---|
| early (steps 1-3) | 30 | 0.325 | 0.083 | 0.990 |
| late_ordinary (steps 19-21, `ai>=0.9`) | 30 | 0.297 | 0.094 | 0.983 |
| late_escape (steps 19-21, `ai<0.9`) | 30 | 0.309 | 0.110 | 0.572 |

| comparison | delta | 95% CI | verdict |
|---|---|---|---|
| late_escape - late_ordinary | +0.012 | [-0.040, +0.064] | indistinguishable from 0 |
| late_ordinary - early | -0.028 | [-0.073, +0.017] | indistinguishable from 0 |
| late_escape - early | -0.017 | [-0.066, +0.033] | indistinguishable from 0 |

`corr(ai_score, craft) = +0.189` (t=1.80, p~0.08) over all 90 — the reward-hacking direction
(escaping tracks worse writing), marginal, not significant.

### What this does NOT establish

1. **Power.** n=30/cohort at sd 0.096 detects only gaps larger than ~0.049, i.e. 15% of
   baseline craft. A real cost smaller than that is invisible here.
2. **Floor effect.** Craft is ~0.30/1.0 in every cohort. A 0.8B model writes poorly to begin
   with, so "quality did not fall" is a weaker claim than it sounds — there was not far to fall.
3. **Not held out.** These are train-split prompts. Directionally this is the *more* direct
   reward-hacking test (it scores the texts that actually beat the detector), but it is not a
   generalization result.

Free text proxies agree: escapes have narrower vocabulary (TTR 0.380 vs 0.431) and run longer
(1343 vs 1169 words), but no non-ASCII drift (54% of rollouts either way) and coherence held at
0.55-0.65 with no spike in `gated_coherence`. The escaping prose is recognizable English that
drifts semantically — "a relentless, suffocating drumbeating against the woodroof" — rather than
collapsing into gibberish. The coherence floor did its job.

## Bug found: the eval overlay measured nothing

All 72 eval rollouts gated on `word_count` with empty stories while generating 949-1512 tokens.
`content` was null and the whole story sat in `reasoning_content`.

prime-rl builds one pool with two clients (`orchestrator/utils.py`):

```
train_client_type="renderer"                 <- our qwen3.5 renderer, enable_thinking=false
eval_client_type="openai_chat_completions"   <- raw chat completions, server-side template
```

So `[orchestrator.renderer]` never reached eval, Qwen3.5's default template left thinking on,
and `extract_story` saw `""`. It failed silently because a gated rollout is a legitimate 0, not
an error — `errors: []` on every one. Fixed in `cc6f859` by setting
`[orchestrator.eval.sampling.extra_body.chat_template_kwargs] enable_thinking = false`, verified
against the real `RLConfig` to cascade to the env entry.

## Cost

| | |
|---|---|
| Pangram, this run | 724 calls x $0.05 = **$36.20** (est. was $43) |
| Pangram, craft judging | $0 — reused rollouts on disk |
| Prime Inference (judge) | $1.31 |

Eval gating for free is why the run came in under estimate: it spent $0.05 instead of $3.60.
Gates run before the API call, so 209 of 933 rollouts cost nothing.
